# Tech Debt Backlog

Engineering investment opportunities identified by the Tech Lead across cycles.
Review when planning — items deferred 5+ cycles signal recurring friction.

| Cycle | Investment | Status |
|-------|-----------|--------|
| 211 | **Quest scaffolding system**: Standardized flag naming, validation script, 3-state flag recipe. Not causing friction — revisit if quest-heavy cycles resume. | pending |
| 217→254 | **Config-driven species generator**: `scripts/generate_species.cjs` — JSON config → 18 files (cry_tables.inc skipped, cry_ids.h handles mapping). Validated: 18/19 check_species pass, idempotency check, dry-run mode. | done (C254) |
| 227→234 | **Palette recolor helper**: CLI tool (reads .pal + color mapping JSON → recolored output). Not blocking — Sprite Designer handles palette creation. | pending |
| 244 | **Rival R119 Stantler_Hoenn Lv25 gap**: Quick fix. | done (C245) |
| 245→253 | **Trainer swap validator → check_all**: `check_trainers` added to `make check_all` target. | done (C254) |
| 254→260 | **26-file species generator**: Extended `generate_species.cjs` from 18-file to 26-file scope — added 8 graphics table handlers. Eliminates ALL manual steps from species addition. | done (C260) |
| 258→266 | **Trainer config generator**: `scripts/generate_trainer.cjs` — JSON config → synchronized trainer_parties.h/trainers.h/opponents.h entries. Create + modify modes, dry-run support. Eliminates 3-file manual sync (failure pattern from C179, C190, C195). 6 deferrals before shipping. | done (C266) |
| 267 | NPC dialogue template generator: The 5 NPC scripts this cycle follow an identical pattern (object_event in map.json + lock/faceplayer/msgbox/release/end in scripts.inc + .string text). If this pattern recurs (and it has, 90+ times), a lightweight generate_npc_dialogue.cjs that takes species name + map + dialogue text JSON and outputs valid map.json entry + .inc script block would cut NPC work from ~15 minutes to ~2 minutes per NPC. Not blocking — evaluate if the pattern stabilizes further. | pending |
| 268 | NPC dialogue template generator: C267 added 5 NPCs following an identical pattern (object_event in map.json + lock/faceplayer/msgbox/release/end in scripts.inc + .string text). A generate_npc_dialogue.cjs taking species, map, coords, sprite, and dialogue as JSON config would cut each NPC from ~15 min to ~2 min. The trainer generator's architecture (JSON config → multi-file atomic writes) is directly reusable. Evaluate if v2.4 involves another dialogue-heavy pass (gym leader narrative → likely yes). | pending |
| 269 | NPC dialogue template generator (generate_npc_dialogue.cjs): The lock/faceplayer/msgbox/release/end + object_event + .string pattern has been replicated 90+ times and proposed in C267-268. This cycle adds 3 more instances. The trainer generator's architecture (JSON config → multi-file atomic writes) is directly reusable. Would cut each NPC from ~15 min to ~2 min. Evaluate building it when a batch of 5+ NPCs is next needed. | pending |
| 270 | NPC dialogue template generator (generate_npc_dialogue.cjs): Proposed C267-269, now 90+ instances of the identical lock/faceplayer/msgbox/release/end + object_event + .string pattern. The trainer generator's architecture (JSON config → multi-file atomic writes) is directly reusable. Would cut each NPC from ~15 min to ~2 min. This cycle modifies existing dialogue (no new NPCs), so not needed now. Build it when next batch of 5+ new NPCs is needed — likely Juan/Wallace cycle. | pending |
| 271 | NPC dialogue template generator (generate_npc_dialogue.cjs): Proposed C267-270, 90+ instances of the identical lock/faceplayer/msgbox/release/end + object_event + .string pattern. The trainer generator's architecture (JSON config → multi-file atomic writes) is directly reusable. Would cut each NPC from ~15 min to ~2 min. This cycle modifies existing dialogue (no new NPCs), so not triggered. Build it when the next batch of 5+ new NPCs is needed — likely the Juan/Wallace cycle or a world-thickening pass. | pending |
