# Tech Debt Backlog

Engineering investment opportunities identified by the Tech Lead across cycles.
Review when planning — items deferred 5+ cycles signal recurring friction.

| Cycle | Investment | Status |
|-------|-----------|--------|
| 211 | **Quest scaffolding system**: Standardized flag naming convention (FLAG_QUEST_{name}_{STAGE}), validation script, documented 3-state flag recipe template. Not causing active friction — revisit if quest-heavy cycles resume (C233 Deoxys Quest II). | pending |
| 217 | **Pipeline rewrite**: Replace add_regional_form.cjs with config-driven species generator. Gap-filler (C222) partially addresses registration. Full pipeline rewrite critical path for #118 regional forms. | planned for C230 |
| 226 | **Player sprite manifest script**: Catalog all Brendan/May sprite + palette files (overworld, front, back, intro, cutscene, rival). Critical for #136 to avoid "missed one sprite" problem. | planned for C227 |
| 227 | Palette recolor helper script — a Node.js tool that reads a .pal file, applies a color mapping (vanilla blue → cyan), and writes the result. If built before C228, the recolor becomes a scripted one-action operation instead of manual edits across 30+ files. Could also handle PNG-embedded palette rewriting. The C227 manifest defines the input; this tool would automate the transform. | pending |
| 228 | Palette recolor helper script — a Node.js/Python tool that reads a .pal/.gbapal file and a color mapping JSON (old RGB → new RGB), and outputs the recolored file. Also handles PNG embedded palette rewriting. Would turn any future palette work (regional form recolors, NPC costumes, seasonal variants) into a one-liner. Estimated cost: ~15 actions. This cycle does the recolor manually since the .pal edits are straightforward text replacements, but a tool would pay forward for C229+ visual work. | pending |
| 229 | Palette recolor helper script — a CLI tool that reads a .pal file and a color mapping JSON, outputs the recolored file. Also handles PNG embedded palette rewriting. Would turn any future palette work (regional form recolors, NPC costumes, seasonal variants) into a one-liner. Deferred from C227 and C228 — still not blocking, but would compound if visual work resumes. | pending |
| 230 | Pipeline rewrite: Replace add_regional_form.cjs with config-driven species generator. Gap-filler (C222) partially addresses registration but the full 19-file process remains manual. Deferred 14 cycles (since C217). Not currently blocking — 17 species added without it — but would pay forward if species velocity increases in v2.1+. Consider acting on this if the next 2 species cycles reveal friction the gap-filler can't handle. | pending |
| 231 | Palette recolor helper script — a CLI tool that reads a .pal file and a color mapping JSON, outputs the recolored file. Also handles PNG embedded palette rewriting. With regional forms coming every cycle (each needing custom palette work from the Sprite Designer), this tool would automate the most tedious part of sprite iteration. Estimated cost: ~15 actions. Deferred since C227 (4 consecutive cycles). Not currently blocking — Sprite Designer handles palette creation programmatically — but would accelerate manual sprite iteration and community-feedback-driven refinements. | pending |
| 232 | RGBA auto-conversion script: A Node.js script (scripts/convert_sprites_indexed.cjs) that reads RGBA PNGs from graphics/pokemon/<name>/, extracts the 16-color palette, and writes indexed PNGs. Would eliminate 5-10 manual actions per species cycle. With Deoxys Quest II (C233) potentially needing encounter sprites, this pays for itself in 2 cycles. Deferred since C227 (palette recolor variant), recommended by Tech Lead as more impactful than the palette recolor helper because it hits the critical path every species cycle. | pending |
| 233 | RGBA auto-conversion script (scripts/convert_sprites_indexed.cjs): Reads RGBA PNGs from graphics/pokemon/<name>/, extracts the 16-color palette, writes indexed PNGs. Eliminates 5-10 manual actions per species cycle. Deferred since C227 (6 cycles). Two species cycles pay it back. Priority should increase when species work resumes (v2.2 custom Deoxys form). | pending |
